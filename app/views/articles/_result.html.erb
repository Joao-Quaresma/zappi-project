<% if params[:action] == 'search' %>
  <div class="results-blck">
    <%= bootstrap_flash %>
  </div>
<% end %>  

<% if @article %>
    <div id="article-search-results" class="well results-blocks col-md-12">
      <table class="table table-striped">
        <thead>
          <tr>
            <th>Categories</th>
            <th>Title</th>
          </tr>
        </thead>
        <tbody>
          <% @article.each do |article| %>
            <tr>
              <td><%= render article.categories %></td>
              <td><%= link_to article.title, article %></td>
            </tr>
          <% end %>
        </tbody>
      </table>
    </div>
<% end %>



