

$('#socialposts').append("<%= escape_javascript(render @socialposts)%>");
$('#paginator').html("<%= escape_javascript(link_to_next_page(@socialposts, 'LOAD MORE', remote: true, id: 'load_more'))%>");
if (!$('#load_more').length) { $('#paginator').remove(); }

