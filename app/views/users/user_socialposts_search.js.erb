$('.user-profile-posts').append("<%= escape_javascript(render @user_socialposts)%>");
$('#paginator').html("<%= escape_javascript(link_to_next_page(@user_socialposts, 'LOAD MORE', remote: true, id: 'load_more'))%>");
if (!$('#load_more').length) { $('#paginator').remove(); }
