<% if params[:action] == 'search' %>
  <div class="results-blck">
    <%= bootstrap_flash %>
  </div>
<% end %>  

<% if @announcement %>
    <div id="announcement-search-results" class="row results-blocks col-xs-12">
      <table class="table table-striped">
        <thead>
          <tr>
            <th class="col-xs-8">Title</th>
            <th class="col-xs-4">Categories</th>
          </tr>
        </thead>
        <tbody>
          <% @announcement.each do |announcement| %>
            <tr>
              <td class="col-xs-8"><%= link_to announcement.title, announcement %></td>
              <td class="col-xs-4"><%= render announcement.categories %></td>
            </tr>
          <% end %>
        </tbody>
      </table>
    </div>
<% end %>